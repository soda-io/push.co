// Generated by CoffeeScript 1.7.1
(function() {
  window.Notifications = (function() {
    function Notifications(parentElem, h, colorClass, bgColorClass) {
      this.parentElem = parentElem;
      this.h = h != null ? h : 300;
      this.colorClass = colorClass != null ? colorClass : "nice-black-90";
      this.bgColorClass = bgColorClass != null ? bgColorClass : "nice-white-40-bg";
      $(this.parentElem).css({
        position: "fixed",
        top: "-" + this.h + "px",
        "z-index": 10000
      });
    }

    Notifications.prototype.notify = function(code, message, bgcol, col) {
      var collapse, icon;
      if (bgcol == null) {
        bgcol = this.bgColorClass;
      }
      if (col == null) {
        col = this.colorClass;
      }
      icon = "";
      switch (code) {
        case null:
          icon = "fa fa-check nice-lime-80-neon";
          break;
        case "error":
          icon = "fa fa-bug nice-red-80-neon";
          break;
        default:
          icon = "fa fa-dot-circle-o nice-yellow-80-neon";
      }
      $(this.parentElem).html("<div class='n-wrapper " + bgcol + "'>\n<div class=\"icon\">\n  <span class='" + icon + "'></span>\n</div>\n<div class='message " + col + "'>\n  " + message + "\n</div>\n</div>");
      $(this.parentElem).animate({
        top: "0px"
      }, 500, "ease");
      collapse = (function(_this) {
        return function() {
          return $(_this.parentElem).animate({
            top: "-" + _this.h + "px"
          }, 500, "ease");
        };
      })(this);
      $(this.parentElem).on("swipe dblclick", function() {
        return collapse();
      });
      return setTimeout((function() {
        return collapse();
      }), 3500);
    };

    return Notifications;

  })();

}).call(this);
